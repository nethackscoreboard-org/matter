FROM cpan-feeder:$LABEL as cpan-mojo:$LABEL
COPY ./cpanfile /tmp
RUN cpanm -l /cpan-mods --installdeps /tmp --notest

FROM feeder-skel:$LABEL as nhdb-mojo:$LABEL
COPY --from=cpan-mojo /cpan-mods /cpan-mods
ARG RUN=run
WORKDIR ${RUN:-}
CMD hypnotoad script/nhs
